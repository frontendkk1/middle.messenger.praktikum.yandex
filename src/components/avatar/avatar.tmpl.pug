if avatar
    img.profile-avatar-img(width="130", height="130", src!= avatar)
else
    img.profile-avatar-img(width="40", height="40", src!= unionImage)

if changeHref
    button.profile-avatar-change
        span Поменять аватар
